
<% @users.each do |user| %>
    <% if user.avatar.attached? %>

<%= image_tag(user.avatar, size: "100x100") %>

<% end %>
    <%= user.email %>
    <%= link_to 'user', "user/#{user.id}" %>
    <hr>
<% end %>
